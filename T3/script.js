'use strict';

try {
  // Replace 'Siek Sophannai' with your full name
  console.log('Student Name: Siek Sophannai');
  // Declare x before assigning a value to it
  let x = 10; 
} catch (error) {
  console.error(error.message); 
}

function demo() {
  try {
    // Declare y before assigning a value to it
    let y = 20; 
  } catch (error) {
    console.error(error.message); 
  }
}